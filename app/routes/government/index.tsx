
export default () => {
    // Get all grant-business matchings through firebase query
    // Create a table with filters to display the data

    return (<div>
        Government grants hereç
    </div>)
}